<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AgeMap</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      background-color: #f4f4f4; /* 北欧風の柔らかな背景色 */
      color: #333; /* 主に使用するテキストカラー */
      padding-top: 20px; /* タイトル下の余白 */
    }
    .container {
      max-width: 800px;
      margin: auto;
      padding: 20px;
      background-color: #fff; /* コンテンツの背景色 */
      box-shadow: 0 0 10px rgba(0,0,0,0.1); /* 軽い影を追加 */
      border-radius: 5px; /* 角を丸める */
    }
    .form-group {
      margin-bottom: 20px;
    }
    .btn-primary {
      background-color: #3f8f8f; /* 北欧風の静かな青 */
      border-color: #3f8f8f;
    }
    .btn-primary:hover {
      background-color: #357e7e; /* ホバー時の色変化 */
      border-color: #357e7e;
    }
    .table-bordered {
      border: 1px solid #ccc; /* 薄い境界線 */
    }
    .table-bordered th,
    .table-bordered td {
      border: 1px solid #ccc;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h2>AgeMap 〜年齢表を作成してみよう〜</h2>

    <!-- タイトル下の余白 -->
    <div style="margin-bottom: 20px;"></div>

    <form id="ageComparisonForm">
      <div class="form-row">
        <div class="col-md-6 mb-3">
          <label for="userName">あなたの名前</label>
          <input type="text" class="form-control" id="userName" name="userName" required>
        </div>
        <div class="col-md-6 mb-3">
          <label for="userBirthYear">あなたの生まれ年</label>
          <input type="number" class="form-control" id="userBirthYear" name="userBirthYear" required>
        </div>
      </div>
      <div class="form-row">
        <div class="col-md-6 mb-3">
          <label for="comparisonName">比較したい人の名前</label>
          <input type="text" class="form-control" id="comparisonName" name="comparisonName" required>
        </div>
        <div class="col-md-6 mb-3">
          <label for="comparisonBirthYear">比較したい人の生まれ年</label>
          <input type="number" class="form-control" id="comparisonBirthYear" name="comparisonBirthYear" required>
        </div>
      </div>
      <button type="submit" class="btn btn-primary">表を作成</button>
    </form>
    
    <!-- 年齢比較結果を表示するテーブル -->
    <table id="ageComparisonTable" class="table table-bordered mt-3">
      <thead>
        <tr>
          <th>西暦</th>
          <th>自分の名前</th>
          <th>比較対象の名前</th>
        </tr>
      </thead>
      <tbody>
        <!-- ここに年齢ごとの結果が追加される -->
      </tbody>
    </table>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="app.js"></script>
</body>
</html>
